language: python
python:
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - pypy
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - coverage run --source=clean-sharp setup.py install
after_success:
  - coveralls