class CleanResult:

    def __init__(self, text, error, code_lines, total_lines):
        self.text = text
        self.error = error
        self.code_lines = code_lines
        self.total_lines = total_lines
